<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ trojan_go_caddy_placeholder_html_title }}</title>
  <style>
    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: #0f172a;
      color: #e2e8f0;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
    }
    main {
      max-width: 40rem;
      padding: 3rem;
      background: rgba(15, 23, 42, 0.85);
      border-radius: 1rem;
      box-shadow: 0 1.5rem 3rem rgba(15, 23, 42, 0.45);
      text-align: center;
    }
    h1 {
      font-size: 2.25rem;
      margin-bottom: 1rem;
    }
    p {
      line-height: 1.6;
      margin-bottom: 0;
    }
  </style>
</head>
<body>
  <main>
    <h1>{{ trojan_go_caddy_full_domain }}</h1>
    <p>{{ trojan_go_caddy_placeholder_html_message }}</p>
  </main>
</body>
</html>
